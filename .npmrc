# pnpm build-script allowlist lives in package.json -> pnpm.onlyBuiltDependencies.

# Hoist @microsoft/* packages to root node_modules so the msteams extension
# can resolve @microsoft/agents-hosting after pnpm prune --prod removes workspace symlinks.
public-hoist-pattern[]=@microsoft/*
