# pnpm build-script allowlist lives in package.json -> pnpm.onlyBuiltDependencies.
# Disable frozen lockfile: Railway uses pnpm@10.32.1 but lockfile was generated
# with pnpm@10.23.0 — config hash mismatch causes ERR_PNPM_LOCKFILE_CONFIG_MISMATCH.
frozen-lockfile=false
